#include<iostream>
#include<cmath>
using namespace std;
int prosto(int n)
{   int a;
    if(a==2 or a==3);
    {
        int d=3;
        while((d<=sqrt(a)) and (a%d!=0)) d=d+2;
        if(a%d!=0);
    }
    return prosto;

}
int main ()
{ int n,m; cin>>n>>m;
cout<<prosto(n,m)<<endl;

    return 0;
}
