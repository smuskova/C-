#include<iostream>
#include<cmath>
using namespace std;
int prosto(int n)
{
    int a=0;
    if(a==2 or a==3)
    {
        int d=3;
        while((d<=sqrt(a)) and (a%d!=0)) d=d+2;
        if(a%d!=0);
        }
    return prosto;

}
int main ()
{
    int b;
    cin>>b;
    for(int i=1; i<=b; i++) cout<<prosto(b/i)<<endl;

    return 0;
}
