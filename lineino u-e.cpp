#include<iostream>
#include<cmath>
using namespace std;
int main ()
{
    int a,b;
    cin>>a>>b; int x;
    if(a==0)
    {
        if(b==0)
        {
            cout<<"vsqko x";
        }
        else(cout<<"nqma r-e");
        }
    else( x=b/a);
        cout<<x<<endl;

        return 0;
}
